[common]
# frp服务器地址（需要你有一个公网服务器）
server_addr = your-server-ip
server_port = 7000

# 认证token（与服务器端保持一致）
token = your-auth-token

[web]
type = http
local_port = 80
custom_domains = your-domain.com

[api]
type = http
local_port = 5000
custom_domains = api.your-domain.com 